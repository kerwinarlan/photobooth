# Render blueprint - one-click deploy for Photobooth (Node + Socket.IO).
# Dashboard -> New -> Blueprint, paste: https://github.com/kerwinarlan/photobooth
# Or: https://dashboard.render.com/select-repo?type=web, pick this repo.
services:
  - type: web
    name: photobooth
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /health
